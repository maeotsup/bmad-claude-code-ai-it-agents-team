Initialize the AI development team for this project by detecting the tech stack (or interviewing the user for new projects) and generating all configuration files.

This command invokes the `/setup` skill. Follow the skill instructions.
